# Secrets & credentials — NEVER commit
Users*git-credentials
.git-credentials
*.git-credentials
.env
.env.local
token.json
credentials.json

# Python
__pycache__/
*.pyc
*.pyo

# Node
node_modules/
npm-debug.log

# IDE / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Build / cache
.cache/
dist/
build/

# Legacy Netlify (GH Pages is current host — keep until manual cleanup)
.netlify/

# Local-only dev cruft
tmp_*.log
tmp_*.py
*.bak
*_backup_*